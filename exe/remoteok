#!/usr/bin/env rails runner
# vim:ft=ruby ts=2 sw=2 sts=2 et fenc=utf-8
# frozen_string_literal: true

begin
  Rails.logger.info { "START exe/remoteok pid:#{Process.pid}" }
  RemoteOK.jobs
rescue StandardError => e
  Rails.logger.error { "ERROR exe/remoteok pid:#{Process.pid} -- #{e.class}: #{e.message}" }
ensure
  Rails.logger.info { "END exe/remoteok pid:#{Process.pid}" }
end
