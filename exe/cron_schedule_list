#!/usr/bin/env rails runner
# vim:ft=ruby ts=2 sw=2 sts=2 et fenc=utf-8
# frozen_string_literal: true

require 'whenever'

def cron_job_list
  @cron_job_list ||= begin
    options = {
      crontab_command: 'crontab',
      file: 'config/schedule.rb',
      cut: 0,
      identifier: '/Users/mike/projects/outlierjobs/core/config/schedule.rb'
    }

    Whenever::JobList.new(options).instance_variable_get(:@jobs)[:default_mailto]
  end
end

binding.pry
puts
