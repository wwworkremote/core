#!/bin/bash
# Ensure we are in the project root
cd "$(dirname "$0")/.." || exit

echo "🚀 Starting Full Job Fetch Pipeline via DataAcquisitionManager..."

# Run all fetchers with force=true to bypass cooldowns
bin/rails runner "puts DataAcquisitionManager.run_all(force: true).to_yaml"

echo "✨ Full Pipeline Complete!"
