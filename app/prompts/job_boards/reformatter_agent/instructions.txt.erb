Task: Reformat the following raw job posting description into clean, readable Markdown.

Rules:
1. Preserve every fact from the original text exactly -- do not add, remove, summarize, or
   editorialize any requirement, responsibility, benefit, or detail.
2. Structure it with Markdown headings (##), bullet lists, and bold text where the source
   implies structure (e.g. a run-on paragraph listing several responsibilities becomes a bullet
   list), but do not invent section headings the original doesn't support.
3. Fix obvious whitespace/formatting artifacts from scraping (run-together words, stray HTML
   entities, inconsistent line breaks) without changing wording.
4. Do not add a title, company name, or any content not present in the source -- this is a
   reformat, not a rewrite or a summary.

Return ONLY the reformatted Markdown. No preamble, no explanation, no code fences around the
whole response.
