<%= form_with(model: llm_chat, url: llm_chats_path, class: "contents") do |form| %>
  <% if llm_chat.errors.any? %>
    <div id="error_explanation" class="bg-red-50 text-red-500 px-3 py-2 font-medium rounded-md mt-3">
      <h2><%= pluralize(llm_chat.errors.count, "error") %> prohibited this llm chat from being saved:</h2>

      <ul class="list-disc ml-6">
        <% llm_chat.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="my-5">
    <%= form.label :model, "Select AI model:" %>
    <%= form.select :model,
        options_for_select(@chat_models.map { |model| [model.label, model.id] }.unshift([default_model_display_name, nil]), @selected_model),
        {},
        class: ["block shadow-sm rounded-md border px-3 py-2 mt-2 w-full", {"border-gray-400 focus:outline-blue-600": llm_chat.errors[:model].none?, "border-red-400 focus:outline-red-600": llm_chat.errors[:model].any?}] %>
  </div>

  <div class="my-5">
    <%= form.label :prompt, "Prompt" %>
    <%= form.text_area :prompt, rows: 4, placeholder: "What would you like to discuss?", autofocus: true,
        class: ["block shadow-sm rounded-md border px-3 py-2 mt-2 w-full", {"border-gray-400 focus:outline-blue-600": llm_chat.errors[:prompt].none?, "border-red-400 focus:outline-red-600": llm_chat.errors[:prompt].any?}] %>
  </div>

  <div class="inline">
    <%= form.submit "Start new llm chat", class: "w-full sm:w-auto rounded-md px-3.5 py-2.5 bg-blue-600 hover:bg-blue-500 text-white inline-block font-medium cursor-pointer" %>
  </div>
<% end %>
