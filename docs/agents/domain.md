# Domain Documentation

This project uses a single-context layout for domain knowledge.

## Consumer Rules
- **Context:** Read `CONTEXT.md` at the root for domain language and core concepts.
- **Decisions:** Read `docs/adr/*.md` for past architectural decisions.
- **Updates:** When a plan crystallizes or a decision is made, update `CONTEXT.md` or create a new ADR.
